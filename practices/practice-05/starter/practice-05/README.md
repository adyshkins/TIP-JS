# Практическая работа № 5. Асинхронные операции и получение данных с сервера

## Данные работы

| Поле | Значение |
|---|---|
| Имя | Заполнить |
| Группа | Заполнить |
| Вариант из ПР2–ПР4 | Заполнить |
| Node.js | Указать фактическую версию |
| Операционная система | Заполнить |

Это шаблон отчёта. Формулировки «Заполнить» и ожидаемые примеры необходимо заменить фактическими результатами.

## Запуск

Рабочая папка: `practice-05` внутри личного репозитория.

Проверка готового API:

```bash
npm run check:api
```

Полный набор после реализации клиента:

```bash
npm test
```

Для ручного сценария необходимы два терминала.

Терминал 1:

```bash
npm start
```

Терминал 2:

```bash
npm run example:fetch
npm run client
```

Адрес API по умолчанию:

```text
http://127.0.0.1:5505/api
```

Если использован другой порт, указать фактические команды и значение переменной `API_URL`.

## Эксперимент с очередями выполнения

До запуска `npm run example:order` записать прогноз порядка меток. После запуска внести фактический порядок и объяснить каждую позицию.

| Этап | Порядок меток | Объяснение |
|---|---|---|
| Прогноз до запуска | Заполнить | Заполнить |
| Фактический результат | Заполнить | Заполнить |

Отдельно указать:

- какие сообщения относятся к синхронному коду;
- какие продолжения попадают в очередь микрозадач;
- почему таймер с задержкой `0` не выполняется немедленно;
- изменится ли вывод, если удалить один из обработчиков Promise.

## Наблюдение за HTTP-ответом

Выполнить `npm run example:fetch` при запущенном API и зафиксировать результат.

| Свойство | Фактическое значение | Что означает |
|---|---|---|
| URL запроса | Заполнить | Заполнить |
| Метод | Заполнить | Заполнить |
| Статус | Заполнить | Заполнить |
| `response.ok` | Заполнить | Заполнить |
| `Content-Type` | Заполнить | Заполнить |
| id полученных задач | Заполнить | Заполнить |
| `meta.filters` | Заполнить | Заполнить |

Добавить сведения из вкладки Network браузера либо из другого HTTP-клиента для запросов `/health`, `/tasks/25` и `/tasks/999`.

## Организация кода

Кратко описать ответственность файлов:

| Файл | Ответственность |
|---|---|
| `api/server.mjs` | Заполнить |
| `src/api-client.js` | Заполнить |
| `src/task-api.js` | Заполнить |
| `src/variant.js` | Заполнить |
| `src/main.js` | Заполнить |

Объяснить, почему `fetchFn` передаётся в `createApiClient`, почему HTTP-ошибки проверяются отдельно от сетевых и почему JSON после разбора всё равно проверяется по контракту.

## Реализация общего клиента

Указать принятые решения для `buildUrl` и `requestJson`.

| Требование | Как реализовано | Проверка |
|---|---|---|
| Нормализация `baseUrl` | Заполнить | Заполнить |
| Кодирование query-параметров | Заполнить | Заполнить |
| Заголовок `Accept` | Заполнить | Заполнить |
| Проверка `response.ok` | Заполнить | Заполнить |
| Проверка `Content-Type` | Заполнить | Заполнить |
| Разбор JSON | Заполнить | Заполнить |
| Ограничение времени | Заполнить | Заполнить |
| Внешняя отмена | Заполнить | Заполнить |

## Классификация ошибок

| Сценарий | Ожидаемый `kind` | Фактические поля `ApiError` | Итог |
|---|---|---|---|
| Задача `999` | `http` | Заполнить | Заполнить |
| `completed=yes` | `http` | Заполнить | Заполнить |
| `/debug/error` | `http` | Заполнить | Заполнить |
| `/debug/invalid-json` | `invalid-response` | Заполнить | Заполнить |
| `/debug/text` | `invalid-response` | Заполнить | Заполнить |
| Не запущен API | `network` | Заполнить | Заполнить |
| Задержка больше тайм-аута | `timeout` | Заполнить | Заполнить |
| Явный `AbortController.abort()` | `aborted` | Заполнить | Заполнить |

Для HTTP-ошибок указать `status`, `code` и `details`, если они присутствуют. Пароли, токены и персональные данные в отчёт не включать.

## Доменный модуль задач

Зафиксировать фактические результаты:

| Вызов | Результат |
|---|---|
| `getTasks()` | Количество и id |
| `getTasks({ completed: false, priority: "high" })` | Количество и id |
| `getTaskById(25)` | Полученный объект |
| `getCategories()` | id и названия |
| `loadInitialData(...)` | Состав объединённого результата |

Перечислить правила проверки задачи, категории, уникальности id и поля `meta.total`.

## Последовательная и параллельная загрузка

Сравнить два сценария получения задач и категорий через `/debug/delay`. Каждый сценарий выполнить минимум три раза с одинаковыми задержками.

| Сценарий | Запуск 1, мс | Запуск 2, мс | Запуск 3, мс | Наблюдение |
|---|---:|---:|---:|---|
| Два последовательных `await` | Заполнить | Заполнить | Заполнить | Заполнить |
| `Promise.all` | Заполнить | Заполнить | Заполнить | Заполнить |

Измерения зависят от компьютера и не сравниваются с чужими значениями. В выводе требуется объяснить причинную связь, а не заявлять точное ускорение для любых условий.

## Общий контрольный сценарий

| Шаг | Действие | Ожидаемый результат | Фактический результат | Итог |
|---:|---|---|---|---|
| 1 | Запустить API и проверить `/health` | `200`, JSON со статусом `ok` | Заполнить | Заполнить |
| 2 | Получить все задачи | 12 задач | Заполнить | Заполнить |
| 3 | Отобрать незавершённые задачи высокого приоритета | id `[4, 13, 34]` | Заполнить | Заполнить |
| 4 | Параллельно получить задачи и категории | Обе части результата доступны | Заполнить | Заполнить |
| 5 | Запросить задачу `999` | `ApiError`, `http`, статус `404` | Заполнить | Заполнить |
| 6 | Остановить API и повторить запрос | `ApiError`, `network` | Заполнить | Заполнить |
| 7 | Запустить API повторно | Клиент снова получает данные | Заполнить | Заполнить |

## Индивидуальный вариант

Указать `variantNumber` и `variantFilters` в `src/variant.js`, выполнить `npm run client` и заполнить таблицу.

| Поле | Значение |
|---|---|
| Номер варианта | Заполнить |
| Переданные фильтры | Заполнить |
| URL запроса | Заполнить |
| Полученные id | Заполнить |
| `meta.total` | Заполнить |
| Соответствие ожидаемому результату | Заполнить |

## Протокол проверки API

Вставить реальный вывод `npm run check:api`.

```text
Здесь будет фактический вывод.
```

## Протокол модульных проверок клиента

Вставить реальный вывод `npm run check:client`.

```text
Здесь будет фактический вывод.
```

## Протокол интеграционных проверок

Вставить реальный вывод `npm run check:integration`.

```text
Здесь будет фактический вывод.
```

## Собственные проверки

| № | Исходное состояние и действие | Ожидаемый результат | Фактический результат | Итог |
|---:|---|---|---|---|
| 1 | Заполнить | Заполнить | Заполнить | Заполнить |
| 2 | Заполнить | Заполнить | Заполнить | Заполнить |
| 3 | Заполнить | Заполнить | Заполнить | Заполнить |

Минимум один случай относится к кодированию query-параметров, один — к HTTP-ошибке, один — к отмене, тайм-ауту или повреждённому ответу.

## Отладка

Указать точки останова до `fetch`, после получения `Response`, перед `response.json()` и в обработчике ошибки. Для одного успешного и одного ошибочного запроса записать значения URL, `response.status`, `response.ok`, `Content-Type`, разобранного payload и итогового `ApiError`.

## Дополнительное задание

Не выполнено / указать выбранное расширение, его контракт и минимум три проверки.

## Вывод

Кратко описать результат, исправленные ошибки и различие между ожиданием Promise, HTTP-ошибкой, сетевой ошибкой, неверным ответом сервера, тайм-аутом и явной отменой запроса.
