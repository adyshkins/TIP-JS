# Практическая работа № 12 — отчёт

**Тема:** Подключение API к базе данных

- ФИО: _заполнить_
- Группа: _заполнить_
- Вариант: _заполнить_
- ОС: _заполнить_
- Node.js / npm: _фактические версии_
- PostgreSQL / psql: _фактические версии_
- Коммит: _указать_

## Цель и результат

_Что подключено и какой результат получен._

## Конфигурация

_Указать адрес, порт, имя базы и роль без пароля. Объяснить назначение `.env.example`, `.env` и `.gitignore`._

## Архитектура

_Показать цепочку React → HTTP API → store → Pool → PostgreSQL. Указать, какие слои не содержат SQL._

## Реализованные запросы

| Операция | SQL и параметры | Фактический результат |
|---|---|---|
| Список и фильтры | _заполнить_ | Не выполнено |
| Чтение по id | _заполнить_ | Не выполнено |
| Создание | _заполнить_ | Не выполнено |
| Частичное обновление | _заполнить_ | Не выполнено |
| Удаление | _заполнить_ | Не выполнено |
| Reset-транзакция | _заполнить_ | Не выполнено |

_Объяснить `$1...$n`, формирование фиксированных SQL-фрагментов, экранирование `%`, `_`, `\` и отображение `category_id AS "categoryId"`._

## Запуск

```bash
npm install
npm start
```

_Привести фактические команды подготовки базы и запуска двух процессов. Секреты не вставлять._

## Проверки

```bash
npm run check:queries
npm run check:api
npm run check:db
npm test
```

- `check:queries`: _не выполнено_
- `check:api`: _не выполнено_
- `check:db`: _не выполнено_
- `npm test`: _не выполнено_
- Перезапуск API сохраняет созданную запись: _не выполнено_
- Откат reset при ошибке: _не выполнено_
- Ручной протокол 1–14: _добавить фактические результаты_

## Индивидуальный вариант

_Начальное условие, HTTP-запросы, id, статусы, изменение в интерфейсе и прямое подтверждение в psql._

## Исправленная ошибка

_Симптом → причина → исправление → повторная проверка._

## Контрольные вопросы

_Ответить на вопросы раздела 12 методички._

## Вывод

_Сопоставить хранение в памяти ПР10 и постоянное хранение ПР12; указать ограничения решения._
