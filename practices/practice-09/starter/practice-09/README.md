# Практическая работа № 9 — отчёт

**Тема:** Создание собственного HTTP API

- ФИО: _заполнить_
- Группа: _заполнить_
- Вариант: _заполнить_
- Node.js / npm: _указать фактические версии_
- Коммит сдаваемой версии: _указать_

## Цель и результат

_Описать цель и фактически реализованные возможности._

## Установка и запуск

Команды выполняются из папки `practice-09`:

```bash
npm ci
npm start
```

API: http://127.0.0.1:5505/api. Проверка окружения: `/api/health`.

Другой порт: `npm start -- 5515`. Режим разработки: `npm run dev`.

Если используется `.env`, запуск: `node --env-file=.env src/server.js`; обычный `npm start` файл не читает.

## Реализация

_Описать обязанности validation, task-service, store, routes, app и server. Объяснить нормализацию false, контроль дубликатов query, id и порядок middleware._

## Контракт

_Перечислить маршруты. Привести один успешный ответ и ошибки 400, 404, 405. Объяснить meta.total и границу между ПР9 и ПР10._

## Индивидуальный вариант

| Запрос | Ожидаемые статус и данные | Фактический результат |
|---|---|---|
| Основной фильтр | _заполнить_ | Не выполнено |
| Отрицательный сценарий | _заполнить_ | Не выполнено |
| Отдельная задача из выборки | _заполнить_ | Не выполнено |
| Отсутствующий id | 404 TASK_NOT_FOUND | Не выполнено |

## Автоматические проверки

```bash
npm run check:unit
npm run check:api
npm test
```

| Набор | Успешно | Неуспешно | Примечание |
|---|---|---|---|
| Модульные | — | — | Не выполнено |
| HTTP | — | — | Не выполнено |

_Добавить фактический итог команд. Заготовка содержит TODO и не должна проходить все проверки до реализации._

## Ручной протокол

_Для сценариев 1–13 из методички записать запрос, ожидаемый и фактический результат. Отдельно указать, выполнялось ли дополнительное чтение из браузерного клиента ПР8._

## Исправленный дефект

_Симптом → причина → исправление → повторная проверка._

## Контрольные вопросы

_Ответить на вопросы раздела 12 методички._

## Вывод

_Что реализовано, чем подтверждено, какие ограничения остались._
