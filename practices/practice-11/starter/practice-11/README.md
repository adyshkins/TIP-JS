# Практическая работа № 11 — отчёт

**Тема:** Проектирование и создание базы данных приложения

- ФИО: _заполнить_
- Группа: _заполнить_
- Вариант: _заполнить_
- PostgreSQL / psql: _фактические версии_
- ОС: _заполнить_
- Имя учебной базы и роль: _заполнить, без пароля_
- Коммит: _указать_

## Цель и результат

_Что спроектировано и выполнено._

## Модель данных

_Две сущности, связь, обязательность полей. Обосновать типы, PK/FK, RESTRICT, CHECK, UNIQUE и индекс. Указать различия categoryId/category_id и проверок строк SQL/JavaScript._

## Подготовка и запуск

_Описать установку, создание роли и базы, фактический адрес/порт без пароля._

Из папки `practice-11`:

```bash
psql -X -h 127.0.0.1 -p 5432 -U tip_app -d tip_tasks -v ON_ERROR_STOP=1 -1 -f sql/01_schema.sql
psql -X -h 127.0.0.1 -p 5432 -U tip_app -d tip_tasks -v ON_ERROR_STOP=1 -1 -f sql/02_seed.sql
psql -X -h 127.0.0.1 -p 5432 -U tip_app -d tip_tasks -v ON_ERROR_STOP=1 -f sql/03_queries.sql
psql -X -h 127.0.0.1 -p 5432 -U tip_app -d tip_tasks -v ON_ERROR_STOP=1 -f sql/04_variant.sql
```

Схема предназначена для пустой базы, seed — для пустых таблиц. Исключения TODO заменяются своим SQL. Указать, что получено после загрузки и как синхронизированы последовательности.

## Запросы

| Запрос | Ожидаемый результат | Фактический результат |
|---|---|---|
| Q1 | 12 задач с названием категории | Не выполнено |
| Q2 | id 4, 34 | Не выполнено |
| Q3 | id 7, 16 | Не выполнено |
| Q4 | Категории 1/2/3: 3/6/3 задачи | Не выполнено |
| Q5 | Выполнено/в работе: 1/2, 2/4, 2/1 | Не выполнено |
| Q6 | Категория 2, 6 задач | Не выполнено |
| Q7 | id 4, categoryId 2 | Не выполнено |
| Вариант | _по методичке_ | Не выполнено |

_Для Q4/Q5 отдельно проверить пустую категорию внутри транзакции и выполнить ROLLBACK._

## Проверки

```bash
psql -X -h 127.0.0.1 -p 5432 -U tip_app -d tip_tasks -v ON_ERROR_STOP=1 -f checks/schema.check.sql
psql -X -h 127.0.0.1 -p 5432 -U tip_app -d tip_tasks -v ON_ERROR_STOP=1 -f examples/transaction.sql
```

- Первый прогон: _не выполнено; ожидается passed 41_
- Повторный прогон: _не выполнено; ожидается passed 41_
- Количество исходных строк после проверок: _не выполнено_
- Транзакция: _не выполнено; ожидается false → true → false_
- Повторное создание во второй пустой базе: _не выполнено_
- Ручной протокол 1–9: _добавить фактические результаты_

Проверки откатывают строки, но могут продвинуть последовательности. Не требовать ровно id 35 после прогонов. Автоматический набор не проверяет тексты запросов Q1–Q7 и варианта.

## Исправленная ошибка

_Симптом → причина → исправление → повторная проверка._

## Контрольные вопросы

_Ответить на вопросы раздела 13._

## Вывод

_Результат, ограничения и следующий этап. В ПР11 Express ещё не подключён к PostgreSQL._
