# Практическая работа № 10 — отчёт

**Тема:** Реализация операций изменения данных и интеграция с клиентом

- ФИО: _заполнить_
- Группа: _заполнить_
- Вариант: _заполнить_
- Node.js / npm: _указать версии_
- Ссылка на выполненный клиент ПР8: _заполнить_
- Коммит согласованных версий: _заполнить_

## Цель и результат

_Описать фактически реализованное._

## Запуск сервера и клиента

Терминал 1, из `practice-10`:

```bash
npm ci
npm start
```

API: http://127.0.0.1:5505/api. Для другого порта: `npm start -- 5515`.

Терминал 2, из выполненной `practice-08`:

```bash
npm ci
npm run dev
```

В локальном `practice-08/.env`: `VITE_API_URL=http://127.0.0.1:5505/api`.

API ПР8 отдельно не запускается. После смены `.env` Vite перезапускается, сборка создаётся заново. Для серверного `.env` вместо `npm start` используется `node --env-file=.env src/server.js`.

## Реализация

_Обязанности task-validation, store, write-routes, json-body и errors. Как назначается id, сохраняются непереданные поля и исключаются частичные изменения._

## Контракт

_Примеры POST 201 с Location, PATCH 200, DELETE 204, ошибки 400 с details. Различие INVALID_JSON и VALIDATION_ERROR._

## Интеграция клиента

_Адрес собственного API, подтверждение через Network, изменённые файлы ПР8 (либо «изменения исходного кода не потребовались»). Поведение формы при ошибке записи и ошибке повторного GET._

## Вариант

| Действие / запрос | Ожидаемый результат | Фактический результат |
|---|---|---|
| Создание | _заполнить_ | Не выполнено |
| Изменение / фильтр | _заполнить_ | Не выполнено |
| Удаление / проверка id | _заполнить_ | Не выполнено |
| Неверный запрос напрямую | Ошибка, данные не изменены | Не выполнено |

## Проверки

Команды из `practice-10`:

```bash
npm run check:read
npm run check:write
npm run check:integration
npm test
```

| Набор | Ожидается | Фактически |
|---|---|---|
| Чтение | 89 успешных, 0 ошибок | Не выполнено |
| Запись | 77 успешных, 0 ошибок | Не выполнено |
| Интеграция HTTP-адаптеров | 8 успешных, 0 ошибок | Не выполнено |
| Всего | 174 успешных, 0 ошибок | Не выполнено |

Заготовка содержит TODO и не проходит весь набор до реализации.

Из `practice-08`: `npm run build`, затем `npm run preview` при работающем API ПР10.

- Сборка: _не выполнено_
- CRUD в preview: _не выполнено_
- Ручной протокол 1–15: _добавить фактические результаты по каждому пункту_
- Успех записи при ошибке повторного чтения: _не выполнено_

## Исправленный дефект

_Симптом → причина → исправление → повторная проверка._

## Контрольные вопросы

_Ответить на вопросы раздела 12 методички._

## Вывод

_Достигнутый результат и ограничения. Объяснить потерю изменений после перезапуска сервера._
