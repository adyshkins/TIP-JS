# ПР7. Создание компонентного интерфейса на React

- Автор:
- Группа:
- Вариант:
- Node.js / npm:

## Запуск

Из папки `practice-07`: `npm ci`, затем `npm run dev`.
`package-lock.json` уже входит в заготовку.
Адрес по умолчанию: http://127.0.0.1:5173/.
Проверки: `npm test`. Сборка: `npm run build`. Просмотр сборки: `npm run preview`.

## Результат

Описать выполненные сценарии и ограничения хранения в памяти.

## Компоненты и состояние

Указать владельцев tasks, filters, editingId, draft и errors. Объяснить передачу props и обратных вызовов, вычисление производных данных, сброс формы.

## Индивидуальный вариант

Начальный фильтр, действие, ожидаемый и фактический результат.

## Автоматизированные проверки

Вставить фактический итог `npm test` и `npm run build`, указать исправленные ошибки. Не записывать ожидаемый результат как уже полученный.

## Ручные проверки

Перенести сценарии из раздела 9 методички.

| Сценарий | Ожидаемый результат | Фактический результат |
|---|---|---|
| | | |

## Контрольные вопросы

Ответы своими словами.

## Вывод

Что удалось освоить, какие ошибки исправлены, что предстоит изменить при подключении API.
