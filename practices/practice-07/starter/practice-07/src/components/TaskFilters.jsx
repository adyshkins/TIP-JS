export default function TaskFilters({ filters, categories, onChange }) {
  // TODO: реализовать контракт компонента.
  return (<section aria-label="Фильтры"><h2>Фильтры</h2><p>TODO: управляемые поля и сброс.</p></section>);
}
