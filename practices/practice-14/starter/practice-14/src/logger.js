const MESSAGE = 'Реализуйте модуль наблюдаемости по разделу 6 методички ПР14.';

export function createLogger(options = {}) {
  void options;
  throw new Error(MESSAGE);
}

export function requestLogger(options = {}) {
  void options;
  throw new Error(MESSAGE);
}
